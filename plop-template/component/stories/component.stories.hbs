import Jal{{properCase name}} from '../src/{{name}}.vue'

export default {
  title: 'Jal{{properCase name}}',
  component: Jal{{properCase name}}
}

export const {{properCase name}} = _ => ({
  components: { Jal{{properCase name}} },
  template: `
    <div>
      <jal-{{name}}></jal-{{name}}>
    </div>
  `
})