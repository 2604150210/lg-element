import Jal{{properCase name}} from './src/{{name}}.vue'

Jal{{properCase name}}.install = Vue => {
  Vue.component(Jal{{properCase name}}.name, Jal{{properCase name}})
}

export default Jal{{properCase name}}
